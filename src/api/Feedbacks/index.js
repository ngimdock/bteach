// Feedback operations
import { db, storage } from '../../firebase'

const getFeebacks = async () => {
  // To do
}

const createFeebacks = async (idUser, data) => {
  // To do
}

export {
  getFeebacks,
  createFeebacks
}